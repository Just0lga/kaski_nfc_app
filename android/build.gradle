allprojects { 
    repositories { 
        google() 
        mavenCentral()
        // Baylan Maven Repository - Doğru repository 
        maven { 
            url = uri("https://repo.baylan.info.tr/nexus/repository/kahramanmaras-release/")
            credentials { 
                username = "kahramanmaras.kaski" 
                password = ")ei.f3rocX6nP:PD" 
            } 
        } 
    } 
} 

rootProject.buildDir = '../build' 

subprojects { 
    project.buildDir = "${rootProject.buildDir}/${project.name}" 
} 

subprojects { 
    project.evaluationDependsOn(':app') 
} 

tasks.register("clean", Delete) { 
    delete rootProject.buildDir 
} 
