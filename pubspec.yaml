name: kaski_nfc_app
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  flutter_svg: ^2.2.0
  provider: ^6.0.5
  uuid: ^3.0.7
  json_annotation: ^4.8.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0
  json_serializable: ^6.6.2
  build_runner: ^2.4.7


flutter_icons:
  android: true
  ios: true
  image_path: "assets/kaski_logo.png"
  
flutter:

  uses-material-design: true

  assets:
    - assets/
